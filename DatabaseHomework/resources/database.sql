create database Vts;

use Vts;

create table Students (id integer not null,first_name Varchar(15),last_name Varchar(20), sing_in Varchar(10), index_num Varchar(10),city Varchar(20), Primary key(id));
